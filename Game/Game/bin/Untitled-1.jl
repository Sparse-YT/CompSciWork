for(int q = 0; q < 1; q++){
 state = states.get(rngS);
            String pop1 = ""  + states.get(rngS).getPop() + "";
            state = states.get(rngS);
            String pop2 = ""  + states.get(rngS).getPop() + "";
            state = states.get(rngS);       
            String pop3 = ""  + states.get(rngS).getPop() + "";
}
for(int w = 0; w < 1; w++){
     state = states.get(rngS);
            String rank1 = ""  + states.get(rngS).getRank() + "";
            state = states.get(rngS);
            String rank2 = ""  + states.get(rngS).getRank() + "";
            state = states.get(rngS);
            String rank3 = ""  + states.get(rngS).getRank() + "";
}
for(int w = 0; w < 1; w++){
     state = states.get(rngS);
            String name1 = ""  + states.get(rngS).getName() + "";
            state = states.get(rngS);
            String name2 = ""  + states.get(rngS).getName() + "";
            state = states.get(rngS);
            String name3 = ""  + states.get(rngS).getName() + "";
}
for(int e = 0; e < 1; e++){
     state = states.get(rngS);
            String abiv1 = ""  + states.get(rngS).getAbiv() + "";
            state = states.get(rngS);
            String abiv2 = ""  + states.get(rngS).getAbiv() + "";
            state = states.get(rngS);
            String abiv3 = ""  + states.get(rngS).getAbiv() + "";

for(int r = 0; r < 1; r++){
     state = states.get(rngS);
            String rank_1 = ""  + states.get(rngS).getRank() + "";
            state = states.get(rngS);
            String rank_2 = ""  + states.get(rngS).getRank() + "";
            state = states.get(rngS);
            String rank_3 = ""  + states.get(rngS).getRank() + "";
}